# ============================================================================
# VyOS Kernel Configuration for dae (eBPF Transparent Proxy)
# ============================================================================
# This configuration adds required kernel options for dae to work properly
# Reference: https://github.com/daeuniverse/dae/blob/main/docs/en/README.md
# ============================================================================

# ----------------------------------------------------------------------------
# Core eBPF Support (Usually already enabled, but ensure they are set)
# ----------------------------------------------------------------------------
CONFIG_BPF=y
CONFIG_BPF_SYSCALL=y
CONFIG_BPF_JIT=y
CONFIG_BPF_JIT_ALWAYS_ON=y

# ----------------------------------------------------------------------------
# BTF (BPF Type Format) Support - CRITICAL for dae
# ----------------------------------------------------------------------------
# This is the most important configuration for dae
CONFIG_DEBUG_INFO=y
CONFIG_DEBUG_INFO_BTF=y
CONFIG_DEBUG_INFO_DWARF_TOOLCHAIN_DEFAULT=y

# IMPORTANT: These must be disabled/not set
# CONFIG_DEBUG_INFO_REDUCED is not set
# CONFIG_DEBUG_INFO_SPLIT is not set
# CONFIG_DEBUG_INFO_COMPRESSED is not set

# ----------------------------------------------------------------------------
# Kprobes Support - CRITICAL for dae
# ----------------------------------------------------------------------------
CONFIG_KPROBES=y
CONFIG_KPROBE_EVENTS=y
CONFIG_UPROBE_EVENTS=y
CONFIG_KPROBE_EVENTS_ON_NOTRACE=y

# ----------------------------------------------------------------------------
# BPF Events - CRITICAL for dae
# ----------------------------------------------------------------------------
CONFIG_BPF_EVENTS=y
CONFIG_PERF_EVENTS=y

# ----------------------------------------------------------------------------
# Traffic Control (TC) with eBPF
# ----------------------------------------------------------------------------
CONFIG_NET_SCHED=y
CONFIG_NET_CLS=y
CONFIG_NET_CLS_ACT=y
CONFIG_NET_CLS_BPF=m
CONFIG_NET_SCH_INGRESS=m
CONFIG_NET_INGRESS=y
CONFIG_NET_EGRESS=y

# ----------------------------------------------------------------------------
# Socket Filter and Stream Parser
# ----------------------------------------------------------------------------
CONFIG_BPF_STREAM_PARSER=y
CONFIG_CGROUP_BPF=y

# ----------------------------------------------------------------------------
# Control Groups (cgroups) - Usually already enabled
# ----------------------------------------------------------------------------
CONFIG_CGROUPS=y
CONFIG_CGROUP_SCHED=y
CONFIG_FAIR_GROUP_SCHED=y

# ----------------------------------------------------------------------------
# Additional eBPF Features (Optional but recommended)
# ----------------------------------------------------------------------------
CONFIG_BPF_LSM=y
CONFIG_HAVE_EBPF_JIT=y
CONFIG_ARCH_WANT_DEFAULT_BPF_JIT=y

# ----------------------------------------------------------------------------
# Debugging and Tracing
# ----------------------------------------------------------------------------
CONFIG_FTRACE=y
CONFIG_FUNCTION_TRACER=y
CONFIG_DYNAMIC_FTRACE=y
CONFIG_HAVE_DYNAMIC_FTRACE=y

# ----------------------------------------------------------------------------
# Networking Support
# ----------------------------------------------------------------------------
CONFIG_INET=y
CONFIG_IPV6=y
CONFIG_NETFILTER=y
CONFIG_NETFILTER_ADVANCED=y
CONFIG_NF_CONNTRACK=y

# ============================================================================
# End of dae configuration
# ============================================================================
